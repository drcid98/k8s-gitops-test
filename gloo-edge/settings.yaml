apiVersion: gloo.solo.io/v1
kind: Settings
metadata:
  name: default
  namespace: gloo-system
spec:
  watchNamespaces:
    - default
    - odoo
    - gloo-system
  discovery:
    fdsMode: WHITELIST
  gateway:
    enableGatewayController: true