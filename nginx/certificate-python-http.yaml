apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: python-http-cert
  namespace: gloo-system
spec:
  secretName: python-http-tls
  issuerRef:
    name: selfsigned-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - python-http.test.local
