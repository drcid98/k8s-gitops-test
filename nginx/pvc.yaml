apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: python-http-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard  #Default de minikube al parecer
  resources:
    requests:
      storage: 1Gi
